/*
    Aqui estaria a forma de se declarar funções e etc, porém eu já sei isso. O que estará presente aqui serão algumas observações, conceitos que antes não conhecia e etc...

    ===============================IÇAMENTO=================================================

    Içamento é algo específico do JavaScript. Significa que ao iniciar o programa, todas as funções (não funciona da mesma forma para funções de callback) serão movidas para o topo do código. Dessa forma, permite que chamamos uma função antes mesmo de declara ela. Em relação a funções de callback, elas não são içadas. 

    ========================================NOMES===================================================

    Por conversão, começamos declarando a primeira palavra de uma função com letra menúscula e as palavras posteriores com letras maiúsculas, ou podemos declarar todas as palavras com letras menúsculas separando as palavras com um underline.

    =============================================BUILT-IN FUNCTIONS========================================

    O que está presente aqui vai além de funções, é um importante conceite do JavaScript. 

    Built-in functions: são funções embutidas. Por exemplo, funções que já vem por padrão, sem a necessidade de criar elas, apenas chama-las, como: console.log(), parseInt(), Alert(), dentre outros. Funções embutidas realizam tarefas simples para qual qualquer objeto globalmentente desde que contenha o valor esperado. 

    Métodos: No começo eu confundi funções embutidas com métodos e propriedades, então para não ocorrer mais esse tipo de confusão, irei explicar o que são métodos e propriedades. Os métodos são algo específico de um tipo de dado, por exemplo: arrays tem seus métodos que são aceitos a apenas a objetos do tipo array. Assim como outros objetos também tem métodos proprios que não podem ser utilizados em um array.

    Propriedade: Propriedade é mais como uma informação genérica que todo o objeto tem, e que pode ser acessado com: Object.propriedade. Um exemplo disso é o Object.length

*/