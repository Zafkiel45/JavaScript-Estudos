const teste = ['akame', 'ga', 'kill'];
console.log(teste.find(function(item){return item === 'ga'}))

// O método find() eé utilizado para retornar um elemento que atente uma determinada condição. OBS: Ele retorna o primeiro elemento que atende a uma determinada condição. 