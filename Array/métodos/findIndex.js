const teste = ['akame', 'ga', 'kill'];
console.log(teste.findIndex(function(item){return item === 'ga'}))

// O método findIndex() é utilizado para retornar o índice do  elemento que atente uma determinada condição. OBS: Ele retorna o primeiro elemento que atende a uma determinada condição. 