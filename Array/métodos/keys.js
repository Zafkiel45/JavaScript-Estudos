const teste = [1,2,2,3,3,4,5,6]

for(const item of Object.keys(teste)) {
    console.log(item)
}

// Retorna somente a Chave de um array ou qualquer outro objeto iterável. Podendo ser útil para associar a chave a algo. 