const nomes = ['Emilly', 'kelly', 'Neném','Neném','Rainha'];

console.log(nomes.indexOf('Rainha'))
console.log(nomes.lastIndexOf('Neném'))

// Tanto o indexOf() quando o LastIndexOf() retorna a posição de um elemento especificado dentro dos parênteses do método, sendo o indexOf() que começa do inicio e vai até o fim, retornando a posição do primeiro elemento que ele encontrar que coincide com o específicado e o lastIndeOf() começa do fim até o inicio retornando o primeiro valor encontrado que coincide.