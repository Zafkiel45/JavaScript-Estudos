const array = ['arroz', 'banana', 'feijão', 'maçã'];

console.log(array.includes('maçã'))


// o método includes serve para verificar se um elemento existe em um array, se existir retorna true, senão existir retornar false. Lembrando que ele pecorre todo o array. 

// Lembrando que includes é um método da E7 (ECMAScript 7 ) e pode não ser suportado por todos os navegadores. 